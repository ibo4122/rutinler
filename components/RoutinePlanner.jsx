"use client";

import { useEffect, useMemo, useState } from "react";

const MONTHS = ["Ocak", "Şubat", "Mart", "Nisan", "Mayıs", "Haziran", "Temmuz", "Ağustos", "Eylül", "Ekim", "Kasım", "Aralık"];
const DAYS = ["Pzt", "Sal", "Çar", "Per", "Cum", "Cmt", "Paz"];
const CATEGORIES = [
  { value: "egitim", label: "Eğitim", icon: "🎓", color: "#60a5fa", bg: "linear-gradient(145deg, rgba(37,99,235,.42), rgba(14,165,233,.16))" },
  { value: "is", label: "İş", icon: "💼", color: "#a78bfa", bg: "linear-gradient(145deg, rgba(124,58,237,.42), rgba(168,85,247,.16))" },
  { value: "gunluk", label: "Günlük", icon: "✅", color: "#34d399", bg: "linear-gradient(145deg, rgba(5,150,105,.38), rgba(16,185,129,.16))" },
  { value: "saglik", label: "Sağlık", icon: "💪", color: "#fb7185", bg: "linear-gradient(145deg, rgba(225,29,72,.38), rgba(251,113,133,.16))" },
  { value: "finans", label: "Finans", icon: "💰", color: "#fbbf24", bg: "linear-gradient(145deg, rgba(217,119,6,.38), rgba(251,191,36,.16))" },
  { value: "kisisel", label: "Kişisel", icon: "✨", color: "#22d3ee", bg: "linear-gradient(145deg, rgba(8,145,178,.38), rgba(34,211,238,.16))" },
  { value: "not", label: "Gün Notu", icon: "📝", color: "#f472b6", bg: "linear-gradient(145deg, rgba(190,24,93,.38), rgba(244,114,182,.16))" },
];
const PRIORITIES = [{ value: "dusuk", label: "Düşük", icon: "🟢" }, { value: "orta", label: "Orta", icon: "🟡" }, { value: "yuksek", label: "Yüksek", icon: "🔴" }];
const STATUSES = [{ value: "pending", label: "Bekliyor", icon: "⬜", color: "#cbd5e1" }, { value: "done", label: "Tamamlandı", icon: "✅", color: "#22c55e" }, { value: "failed", label: "Tamamlanamadı", icon: "❌", color: "#ef4444" }];
const GOAL_COLORS = [{ value: "blue", label: "Mavi", color: "#60a5fa" }, { value: "purple", label: "Mor", color: "#a78bfa" }, { value: "green", label: "Yeşil", color: "#34d399" }, { value: "yellow", label: "Sarı", color: "#fbbf24" }, { value: "pink", label: "Pembe", color: "#f472b6" }, { value: "cyan", label: "Camgöbeği", color: "#22d3ee" }];
const MONTH_COLORS = ["#2563eb", "#7c3aed", "#059669", "#d97706", "#e11d48", "#0891b2", "#65a30d", "#ea580c", "#4f46e5", "#db2777", "#0f766e", "#64748b"];
const glass = { border: "1px solid rgba(255,255,255,.14)", boxShadow: "0 22px 55px rgba(0,0,0,.34), inset 0 1px 0 rgba(255,255,255,.10)", backdropFilter: "blur(14px)" };
const inputStyle = { width: "100%", minWidth: 0, boxSizing: "border-box", border: "1px solid rgba(255,255,255,.22)", background: "rgba(2,6,23,.58)", color: "#f8fafc", borderRadius: "14px", padding: "11px 12px", outline: "none" };
const smallButton = { border: "1px solid rgba(255,255,255,.22)", background: "rgba(15,23,42,.66)", color: "#f8fafc", borderRadius: "9px", padding: "6px 8px", cursor: "pointer", fontWeight: 800, fontSize: "11px", minWidth: 0 };

function pad(value) { return String(value).padStart(2, "0"); }
function localISO(date) { return `${date.getFullYear()}-${pad(date.getMonth() + 1)}-${pad(date.getDate())}`; }
function todayISO() { return localISO(new Date()); }
function dateFromISO(value) { const [y, m, d] = String(value || todayISO()).split("-").map(Number); return new Date(y || new Date().getFullYear(), (m || 1) - 1, d || 1, 12, 0, 0, 0); }
function addDays(date, amount) { const next = new Date(date); next.setDate(next.getDate() + amount); return next; }
function startOfWeek(date) { const next = new Date(date); const day = next.getDay() || 7; next.setDate(next.getDate() - day + 1); next.setHours(12, 0, 0, 0); return next; }
function weekNumber(date) { const target = new Date(Date.UTC(date.getFullYear(), date.getMonth(), date.getDate())); const dayNumber = target.getUTCDay() || 7; target.setUTCDate(target.getUTCDate() + 4 - dayNumber); const yearStart = new Date(Date.UTC(target.getUTCFullYear(), 0, 1)); return Math.ceil(((target - yearStart) / 86400000 + 1) / 7); }
function buildWeeks(year) { const first = new Date(Number(year), 0, 1, 12); const last = new Date(Number(year), 11, 31, 12); let cursor = startOfWeek(first); const weeks = []; while (cursor <= last) { const days = Array.from({ length: 7 }, (_, index) => addDays(cursor, index)); const visibleDay = days.find((day) => day.getFullYear() === Number(year)) || days[0]; weeks.push({ id: `${year}-${localISO(cursor)}`, number: weekNumber(cursor), monthIndex: visibleDay.getMonth(), monthName: MONTHS[visibleDay.getMonth()], days }); cursor = addDays(cursor, 7); } return weeks; }
function categoryInfo(value) { return CATEGORIES.find((item) => item.value === value) || CATEGORIES[2]; }
function priorityInfo(value) { return PRIORITIES.find((item) => item.value === value) || PRIORITIES[1]; }
function statusInfo(value) { return STATUSES.find((item) => item.value === value) || STATUSES[0]; }
function goalColorInfo(value) { return GOAL_COLORS.find((item) => item.value === value) || GOAL_COLORS[0]; }
function formatDate(value) { const date = dateFromISO(value); return `${date.getDate()} ${MONTHS[date.getMonth()]}`; }
function normalizeTask(item) { const status = item?.status || (item?.failed ? "failed" : item?.done || item?.completed ? "done" : "pending"); return { id: String(item?.id || Date.now() + Math.random()), title: item?.title || item?.name || "İsimsiz iş", category: item?.category || "gunluk", priority: item?.priority || "orta", date: item?.date || item?.dueDate || todayISO(), note: item?.note || "", status }; }
function normalizeGoal(item) { return { id: String(item?.id || Date.now() + Math.random()), title: item?.title || "Yeni Hedef", progress: Math.max(0, Math.min(100, Number(item?.progress || 0))), color: item?.color || "blue", note: item?.note || "" }; }
function Field({ label, children }) { return <label style={{ display: "flex", flexDirection: "column", gap: "7px", minWidth: 0 }}><span style={{ color: "#e2e8f0", fontSize: "12px", fontWeight: 900 }}>{label}</span>{children}</label>; }
function Select({ value, onChange, options }) { return <select value={value} onChange={(event) => onChange(event.target.value)} style={inputStyle}>{options.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select>; }

export default function RoutinePlanner({ routines, setRoutines }) {
  const data = Array.isArray(routines) ? routines : [];
  const tasks = data.filter((item) => item?.recordType !== "goal").map(normalizeTask);
  const goals = data.filter((item) => item?.recordType === "goal").map(normalizeGoal);
  const currentYear = new Date().getFullYear();
  const [year, setYear] = useState(currentYear);
  const [month, setMonth] = useState("all");
  const [statusFilter, setStatusFilter] = useState("all");
  const [categoryFilter, setCategoryFilter] = useState("all");
  const [priorityFilter, setPriorityFilter] = useState("all");
  const [search, setSearch] = useState("");
  const [selectedDate, setSelectedDate] = useState(null);
  const [dayNote, setDayNote] = useState("");
  const [quickTitle, setQuickTitle] = useState("");
  const [quickCategory, setQuickCategory] = useState("gunluk");
  const [quickPriority, setQuickPriority] = useState("orta");
  const [taskForm, setTaskForm] = useState({ title: "", category: "egitim", priority: "orta", date: todayISO(), note: "" });
  const [goalForm, setGoalForm] = useState({ title: "", progress: "0", color: "blue", note: "" });
  const [editTaskId, setEditTaskId] = useState(null);
  const [editTask, setEditTask] = useState({ title: "", category: "gunluk", priority: "orta", date: todayISO(), note: "" });
  const [editGoalId, setEditGoalId] = useState(null);
  const [editGoal, setEditGoal] = useState({ title: "", progress: "0", color: "blue", note: "" });
  const [collapsedMonths, setCollapsedMonths] = useState({});
  const [openPanels, setOpenPanels] = useState({ hero: true, goals: true, add: true, filters: true, months: true, calendar: true, dayNote: true, upcoming: true, goalSummary: true });

  useEffect(() => { try { const saved = window.localStorage.getItem("routineCollapsedMonthsV2"); if (saved) setCollapsedMonths(JSON.parse(saved)); } catch (error) { console.warn("Ay kapatma ayarı okunamadı", error); } }, []);
  useEffect(() => { try { window.localStorage.setItem("routineCollapsedMonthsV2", JSON.stringify(collapsedMonths)); } catch (error) { console.warn("Ay kapatma ayarı kaydedilemedi", error); } }, [collapsedMonths]);

  const weeks = useMemo(() => buildWeeks(year), [year]);
  const filtered = useMemo(() => tasks.filter((task) => { const date = dateFromISO(task.date); return date.getFullYear() === Number(year) && (month === "all" || date.getMonth() === Number(month)) && (statusFilter === "all" || task.status === statusFilter) && (categoryFilter === "all" || task.category === categoryFilter) && (priorityFilter === "all" || task.priority === priorityFilter) && (!search.trim() || `${task.title} ${task.note}`.toLowerCase().includes(search.toLowerCase())); }), [tasks, year, month, statusFilter, categoryFilter, priorityFilter, search]);
  const byDate = useMemo(() => filtered.reduce((map, task) => { if (!map[task.date]) map[task.date] = []; map[task.date].push(task); return map; }, {}), [filtered]);
  const selectedDateTasks = selectedDate ? byDate[selectedDate] || [] : [];
  const doneCount = filtered.filter((task) => task.status === "done").length;
  const failedCount = filtered.filter((task) => task.status === "failed").length;
  const goalAverage = goals.length ? Math.round(goals.reduce((sum, goal) => sum + goal.progress, 0) / goals.length) : 0;
  const upcoming = tasks.filter((task) => task.status === "pending" && dateFromISO(task.date) >= dateFromISO(todayISO())).sort((a, b) => a.date.localeCompare(b.date)).slice(0, 10);

  const togglePanel = (key) => setOpenPanels((current) => ({ ...current, [key]: !current[key] }));
  const toggleMonth = (index) => setCollapsedMonths((current) => ({ ...current, [index]: !current[index] }));
  const addItem = (payload) => setRoutines?.((current) => [{ id: String(Date.now()), status: "pending", done: false, completed: false, failed: false, ...payload }, ...(Array.isArray(current) ? current : [])]);
  const updateItem = (id, patch) => setRoutines?.((current) => (Array.isArray(current) ? current : []).map((item) => String(item.id) === String(id) ? { ...item, ...patch } : item));
  const deleteItem = (id) => setRoutines?.((current) => (Array.isArray(current) ? current : []).filter((item) => String(item.id) !== String(id)));
  const addTask = () => { const title = taskForm.title.trim(); if (!title) return alert("Rutin / iş adı gir."); addItem({ ...taskForm, title, note: taskForm.note.trim(), date: taskForm.date || selectedDate || todayISO() }); setTaskForm({ title: "", category: taskForm.category, priority: "orta", date: selectedDate || todayISO(), note: "" }); };
  const addGoal = () => { const title = goalForm.title.trim(); if (!title) return alert("Hedef adı gir."); addItem({ recordType: "goal", title, progress: Math.max(0, Math.min(100, Number(goalForm.progress || 0))), color: goalForm.color, note: goalForm.note.trim() }); setGoalForm({ title: "", progress: "0", color: goalForm.color, note: "" }); };
  const addQuick = () => { if (!selectedDate) return alert("Önce takvimden gün seç."); const title = quickTitle.trim(); if (!title) return alert("Başlık gir."); addItem({ title, category: quickCategory, priority: quickPriority, date: selectedDate, note: "" }); setQuickTitle(""); };
  const addNote = () => { if (!selectedDate) return alert("Önce takvimden gün seç."); const note = dayNote.trim(); if (!note) return alert("Not gir."); addItem({ title: `Gün Notu - ${formatDate(selectedDate)}`, category: "not", priority: "orta", date: selectedDate, note }); setDayNote(""); };
  const selectDay = (dateKey) => { if (selectedDate === dateKey) { setSelectedDate(null); setQuickTitle(""); setDayNote(""); setOpenPanels((current) => ({ ...current, dayNote: false })); return; } setSelectedDate(dateKey); setTaskForm((current) => ({ ...current, date: dateKey })); setOpenPanels((current) => ({ ...current, dayNote: true })); };
  const changeStatus = (id, status) => updateItem(id, { status, done: status === "done", completed: status === "done", failed: status === "failed" });
  const startTaskEdit = (task) => { setEditTaskId(task.id); setEditTask({ title: task.title, category: task.category, priority: task.priority, date: task.date, note: task.note || "" }); };
  const saveTaskEdit = (id) => { const title = editTask.title.trim(); if (!title) return alert("Başlık boş olamaz."); updateItem(id, { ...editTask, title, note: editTask.note.trim() }); setEditTaskId(null); };
  const startGoalEdit = (goal) => { setEditGoalId(goal.id); setEditGoal({ title: goal.title, progress: String(goal.progress), color: goal.color, note: goal.note || "" }); };
  const saveGoalEdit = (id) => { const title = editGoal.title.trim(); if (!title) return alert("Hedef adı boş olamaz."); updateItem(id, { title, progress: Math.max(0, Math.min(100, Number(editGoal.progress || 0))), color: editGoal.color, note: editGoal.note.trim() }); setEditGoalId(null); };

  return <section style={{ display: "grid", gridTemplateColumns: "minmax(0, 1fr) 360px", gap: 22, alignItems: "start" }}>
    <div>
      <Panel title="Rutin Takvimi" open={openPanels.hero} onToggle={() => togglePanel("hero")} gradient="linear-gradient(135deg, rgba(30,64,175,.88), rgba(88,28,135,.82), rgba(15,23,42,.92))"><div style={{ display: "flex", justifyContent: "space-between", gap: 16, flexWrap: "wrap" }}><div><h2 style={{ margin: 0, color: "#fff", fontSize: 34 }}>{year} Planı</h2><p style={{ color: "#dbeafe" }}>Ay kapatma seçimi kalıcıdır. Tarih seçilince sadece o tarihin kayıtları görünür.</p></div><Field label="Yıl"><Select value={year} onChange={setYear} options={[currentYear - 1, currentYear, currentYear + 1].map((item) => ({ value: item, label: String(item) }))} /></Field></div><div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(130px, 1fr))", gap: 12, marginTop: 18 }}><Stat label="Rutin" value={filtered.length} color="#60a5fa" /><Stat label="Tamamlandı" value={doneCount} color="#22c55e" /><Stat label="Tamamlanamadı" value={failedCount} color="#ef4444" /><Stat label="Hedef Ort." value={`%${goalAverage}`} color="#a78bfa" /></div></Panel>
      <Panel title="Hedeflerim" open={openPanels.goals} onToggle={() => togglePanel("goals")} gradient="linear-gradient(145deg, rgba(49,46,129,.88), rgba(8,47,73,.82), rgba(15,23,42,.88))"><div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(160px, 1fr))", gap: 12, marginBottom: 14 }}><Field label="Hedef"><input style={inputStyle} value={goalForm.title} onChange={(event) => setGoalForm((current) => ({ ...current, title: event.target.value }))} placeholder="SQL, Saz, İngilizce" /></Field><Field label="İlerleme %"><input style={inputStyle} type="number" min="0" max="100" value={goalForm.progress} onChange={(event) => setGoalForm((current) => ({ ...current, progress: event.target.value }))} /></Field><Field label="Renk"><Select value={goalForm.color} onChange={(value) => setGoalForm((current) => ({ ...current, color: value }))} options={GOAL_COLORS.map((item) => ({ value: item.value, label: item.label }))} /></Field><Field label="Not"><input style={inputStyle} value={goalForm.note} onChange={(event) => setGoalForm((current) => ({ ...current, note: event.target.value }))} placeholder="Opsiyonel" /></Field><button type="button" style={primaryButton("linear-gradient(135deg,#c4b5fd,#67e8f9,#fef08a)")} onClick={addGoal}>Hedef Ekle</button></div><div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(260px, 1fr))", gap: 12 }}>{goals.length === 0 ? <Empty text="Henüz hedef yok." /> : goals.map((goal) => <GoalCard key={goal.id} goal={goal} editId={editGoalId} edit={editGoal} setEdit={setEditGoal} onEdit={startGoalEdit} onSave={saveGoalEdit} onCancel={() => setEditGoalId(null)} onDelete={deleteItem} />)}</div></Panel>
      <Panel title="Yeni Rutin / İş Ekle" open={openPanels.add} onToggle={() => togglePanel("add")} gradient="linear-gradient(145deg, rgba(6,78,59,.88), rgba(15,23,42,.86))"><div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(180px, 1fr))", gap: 12 }}><Field label="Başlık"><input style={inputStyle} value={taskForm.title} onChange={(event) => setTaskForm((current) => ({ ...current, title: event.target.value }))} /></Field><Field label="Kategori"><Select value={taskForm.category} onChange={(value) => setTaskForm((current) => ({ ...current, category: value }))} options={CATEGORIES.map((item) => ({ value: item.value, label: `${item.icon} ${item.label}` }))} /></Field><Field label="Öncelik"><Select value={taskForm.priority} onChange={(value) => setTaskForm((current) => ({ ...current, priority: value }))} options={PRIORITIES.map((item) => ({ value: item.value, label: `${item.icon} ${item.label}` }))} /></Field><Field label="Tarih"><input style={inputStyle} type="date" value={taskForm.date} onChange={(event) => setTaskForm((current) => ({ ...current, date: event.target.value }))} /></Field><Field label="Not"><input style={inputStyle} value={taskForm.note} onChange={(event) => setTaskForm((current) => ({ ...current, note: event.target.value }))} /></Field><button type="button" style={primaryButton("linear-gradient(135deg,#fef08a,#34d399,#22d3ee)")} onClick={addTask}>Ekle</button></div></Panel>
      <Panel title="Filtreler" open={openPanels.filters} onToggle={() => togglePanel("filters")} gradient="linear-gradient(145deg, rgba(120,53,15,.88), rgba(88,28,135,.70), rgba(15,23,42,.86))"><div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))", gap: 12 }}><Field label="Ay"><Select value={month} onChange={setMonth} options={[{ value: "all", label: "Tüm Aylar" }, ...MONTHS.map((item, index) => ({ value: String(index), label: item }))]} /></Field><Field label="Durum"><Select value={statusFilter} onChange={setStatusFilter} options={[{ value: "all", label: "Tümü" }, ...STATUSES.map((item) => ({ value: item.value, label: `${item.icon} ${item.label}` }))]} /></Field><Field label="Kategori"><Select value={categoryFilter} onChange={setCategoryFilter} options={[{ value: "all", label: "Tüm Kategoriler" }, ...CATEGORIES.map((item) => ({ value: item.value, label: `${item.icon} ${item.label}` }))]} /></Field><Field label="Öncelik"><Select value={priorityFilter} onChange={setPriorityFilter} options={[{ value: "all", label: "Tüm Öncelikler" }, ...PRIORITIES.map((item) => ({ value: item.value, label: `${item.icon} ${item.label}` }))]} /></Field><Field label="Arama"><input style={inputStyle} value={search} onChange={(event) => setSearch(event.target.value)} /></Field></div></Panel>
      <Panel title="Aylar" open={openPanels.months} onToggle={() => togglePanel("months")} gradient="linear-gradient(145deg, rgba(30,41,59,.90), rgba(49,46,129,.64), rgba(15,23,42,.86))"><div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(140px, 1fr))", gap: 12 }}><MonthCard title="Tüm Yıl" count={filtered.length} active={month === "all"} onClick={() => setMonth("all")} />{MONTHS.map((item, index) => <MonthCard key={item} title={item} count={tasks.filter((task) => dateFromISO(task.date).getFullYear() === Number(year) && dateFromISO(task.date).getMonth() === index).length} active={month === String(index)} collapsed={Boolean(collapsedMonths[index])} onClick={() => setMonth(String(index))} onToggle={() => toggleMonth(index)} color={MONTH_COLORS[index]} />)}</div></Panel>
      <Panel title="Haftalık Takvim" open={openPanels.calendar} onToggle={() => togglePanel("calendar")} gradient="linear-gradient(145deg, rgba(8,47,73,.88), rgba(15,23,42,.88))"><div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(420px, 1fr))", gap: 16 }}>{weeks.filter((week) => { const monthVisible = (month === "all" || week.monthIndex === Number(month)) && !collapsedMonths[week.monthIndex]; const selectedWeekVisible = !selectedDate || week.days.some((day) => localISO(day) === selectedDate); return monthVisible && selectedWeekVisible; }).map((week) => { const weekTasks = week.days.flatMap((day) => byDate[localISO(day)] || []); const selectedInWeek = selectedDate && week.days.some((day) => localISO(day) === selectedDate); const visibleTasks = selectedDate ? (selectedInWeek ? selectedDateTasks : []) : weekTasks; return <div key={week.id} style={{ ...glass, background: `linear-gradient(145deg, ${MONTH_COLORS[week.monthIndex]}44, rgba(15,23,42,.76))`, borderRadius: 24, padding: 16 }}><div style={{ display: "flex", justifyContent: "space-between", marginBottom: 12 }}><strong style={{ color: "#fff" }}>{week.monthName}</strong><span style={{ color: "#dbeafe", fontWeight: 900 }}>{week.number}. Hafta</span></div><div style={{ display: "grid", gridTemplateColumns: "repeat(7, minmax(0, 1fr))", gap: 6, marginBottom: 12 }}>{week.days.map((day, index) => { const key = localISO(day); const dayTasks = byDate[key] || []; const selected = key === selectedDate; return <button type="button" key={key} onClick={() => selectDay(key)} style={dayStyle(selected, key === todayISO())}><strong>{DAYS[index]}</strong><div>{day.getDate()}</div><div style={{ display: "flex", gap: 3, flexWrap: "wrap", marginTop: 5 }}>{dayTasks.slice(0, 4).map((task) => <span key={task.id} style={{ width: 7, height: 7, borderRadius: 99, background: statusInfo(task.status).color }} />)}</div></button>; })}</div>{selectedInWeek && <QuickEntry selectedDate={selectedDate} quickTitle={quickTitle} setQuickTitle={setQuickTitle} quickCategory={quickCategory} setQuickCategory={setQuickCategory} quickPriority={quickPriority} setQuickPriority={setQuickPriority} addQuick={addQuick} />}<div style={{ display: "flex", flexDirection: "column", gap: 10 }}>{visibleTasks.length === 0 ? <Empty text={selectedInWeek ? "Seçili tarihe ait rutin yok." : "Bu haftaya kayıt yok."} /> : visibleTasks.map((task) => <TaskCard key={task.id} task={task} editId={editTaskId} edit={editTask} setEdit={setEditTask} onEdit={startTaskEdit} onSave={saveTaskEdit} onCancel={() => setEditTaskId(null)} onStatus={changeStatus} onDelete={deleteItem} />)}</div></div>; })}</div></Panel>
      {selectedDate && <Panel title={`Seçili Gün Notu - ${formatDate(selectedDate)}`} open={openPanels.dayNote} onToggle={() => togglePanel("dayNote")} gradient="linear-gradient(145deg, rgba(131,24,67,.88), rgba(15,23,42,.88))"><div style={{ display: "grid", gridTemplateColumns: "minmax(0, 1fr) 160px", gap: 12 }}><Field label="Güne Not Ekle"><textarea style={{ ...inputStyle, minHeight: 90 }} value={dayNote} onChange={(event) => setDayNote(event.target.value)} /></Field><button type="button" style={primaryButton("linear-gradient(135deg,#f9a8d4,#fef08a)")} onClick={addNote}>Kaydet</button></div><div style={{ marginTop: 14, display: "flex", flexDirection: "column", gap: 10 }}>{selectedDateTasks.length === 0 ? <Empty text="Bu güne ait kayıt yok." /> : selectedDateTasks.map((task) => <TaskCard key={task.id} task={task} editId={editTaskId} edit={editTask} setEdit={setEditTask} onEdit={startTaskEdit} onSave={saveTaskEdit} onCancel={() => setEditTaskId(null)} onStatus={changeStatus} onDelete={deleteItem} />)}</div></Panel>}
    </div>
    <aside><Panel title="Yaklaşan İşler" open={openPanels.upcoming} onToggle={() => togglePanel("upcoming")} gradient="linear-gradient(145deg, rgba(49,46,129,.88), rgba(15,23,42,.88))"><div style={{ display: "flex", flexDirection: "column", gap: 10 }}>{upcoming.length ? upcoming.map((task) => <SmallTask key={task.id} task={task} />) : <Empty text="Yaklaşan iş yok." />}</div></Panel><Panel title="Hedef Özeti" open={openPanels.goalSummary} onToggle={() => togglePanel("goalSummary")} gradient="linear-gradient(145deg, rgba(88,28,135,.88), rgba(15,23,42,.88))"><Empty text={`Toplam hedef: ${goals.length} • Ortalama ilerleme: %${goalAverage}`} />{goals.slice(0, 5).map((goal) => <GoalMini key={goal.id} goal={goal} />)}</Panel></aside>
  </section>;
}

function Panel({ title, children, open, onToggle, gradient }) { return <div style={{ ...glass, background: gradient, borderRadius: 26, padding: 20, marginBottom: 18 }}><button type="button" onClick={onToggle} style={{ width: "100%", border: 0, background: "transparent", color: "#fff", padding: 0, marginBottom: open ? 16 : 0, display: "flex", justifyContent: "space-between", alignItems: "center", cursor: "pointer", textAlign: "left" }}><h3 style={{ margin: 0, fontSize: 20 }}>{title}</h3><span style={{ width: 34, height: 34, borderRadius: 12, display: "grid", placeItems: "center", background: "rgba(255,255,255,.14)", fontWeight: 900 }}>{open ? "−" : "+"}</span></button>{open ? children : null}</div>; }
function Stat({ label, value, color }) { return <div style={{ ...glass, background: `${color}33`, borderRadius: 20, padding: 16 }}><span style={{ color: "#dbeafe", fontSize: 12, fontWeight: 800 }}>{label}</span><strong style={{ display: "block", color: "#fff", fontSize: 24, marginTop: 6 }}>{value}</strong></div>; }
function Empty({ text }) { return <div style={{ padding: 13, borderRadius: 18, background: "rgba(2,6,23,.38)", border: "1px solid rgba(255,255,255,.12)", color: "#cbd5e1", fontSize: 12 }}>{text}</div>; }
function primaryButton(background) { return { border: 0, borderRadius: 16, padding: "12px 14px", color: "#08111f", fontWeight: 900, cursor: "pointer", background, alignSelf: "end" }; }
function dayStyle(selected, today) { return { minHeight: 52, borderRadius: 14, padding: 6, background: selected ? "rgba(255,255,255,.28)" : today ? "rgba(255,255,255,.18)" : "rgba(2,6,23,.38)", border: selected ? "2px solid rgba(255,255,255,.88)" : "1px solid rgba(255,255,255,.12)", color: "#fff", fontSize: 11, cursor: "pointer", textAlign: "left" }; }
function MonthCard({ title, count, active, collapsed, onClick, onToggle, color = "#2563eb" }) { return <div style={{ ...glass, background: `linear-gradient(145deg, ${color}44, rgba(15,23,42,.72))`, borderRadius: 20, padding: 12, transform: active ? "translateY(-4px)" : "none" }}><button type="button" onClick={onClick} style={{ border: 0, background: "transparent", color: "#fff", textAlign: "left", width: "100%", cursor: "pointer" }}><strong>{title}</strong><span style={{ display: "block", color: "#dbeafe", fontSize: 12, marginTop: 5 }}>{count} kayıt</span></button>{onToggle && <button type="button" onClick={onToggle} style={{ ...smallButton, width: "100%", marginTop: 10 }}>{collapsed ? "Ayı Aç" : "Ayı Kapat"}</button>}</div>; }
function QuickEntry({ selectedDate, quickTitle, setQuickTitle, quickCategory, setQuickCategory, quickPriority, setQuickPriority, addQuick }) { return <div style={{ ...glass, background: "rgba(2,6,23,.42)", borderRadius: 18, padding: 12, marginBottom: 12 }}><strong style={{ color: "#fff", display: "block", marginBottom: 10 }}>Hızlı giriş - {formatDate(selectedDate)}</strong><div style={{ display: "grid", gridTemplateColumns: "minmax(0,1fr) 130px 120px 90px", gap: 8 }}><input style={inputStyle} value={quickTitle} onChange={(event) => setQuickTitle(event.target.value)} placeholder="Bu güne iş / not yaz" /><Select value={quickCategory} onChange={setQuickCategory} options={CATEGORIES.map((item) => ({ value: item.value, label: `${item.icon} ${item.label}` }))} /><Select value={quickPriority} onChange={setQuickPriority} options={PRIORITIES.map((item) => ({ value: item.value, label: `${item.icon} ${item.label}` }))} /><button type="button" style={primaryButton("linear-gradient(135deg,#bfdbfe,#67e8f9)")} onClick={addQuick}>Ekle</button></div></div>; }
function GoalCard({ goal, editId, edit, setEdit, onEdit, onSave, onCancel, onDelete }) { const color = goalColorInfo(goal.color); const isEdit = String(editId || "") === String(goal.id); if (isEdit) return <div style={{ ...glass, background: `${color.color}22`, borderColor: `${color.color}66`, borderRadius: 22, padding: 16 }}><div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(140px,1fr))", gap: 10 }}><Field label="Hedef"><input style={inputStyle} value={edit.title} onChange={(event) => setEdit((current) => ({ ...current, title: event.target.value }))} /></Field><Field label="İlerleme %"><input style={inputStyle} type="number" min="0" max="100" value={edit.progress} onChange={(event) => setEdit((current) => ({ ...current, progress: event.target.value }))} /></Field><Field label="Renk"><Select value={edit.color} onChange={(value) => setEdit((current) => ({ ...current, color: value }))} options={GOAL_COLORS.map((item) => ({ value: item.value, label: item.label }))} /></Field><Field label="Not"><input style={inputStyle} value={edit.note} onChange={(event) => setEdit((current) => ({ ...current, note: event.target.value }))} /></Field></div><div style={{ display: "flex", gap: 8, justifyContent: "flex-end", marginTop: 12 }}><button type="button" style={smallButton} onClick={() => onSave(goal.id)}>Kaydet</button><button type="button" style={smallButton} onClick={onCancel}>Vazgeç</button><button type="button" style={{ ...smallButton, color: "#fecaca" }} onClick={() => onDelete(goal.id)}>Sil</button></div></div>; return <div style={{ ...glass, background: `${color.color}22`, borderColor: `${color.color}66`, borderRadius: 22, padding: 16 }}><div style={{ display: "flex", justifyContent: "space-between", gap: 12 }}><div style={{ minWidth: 0 }}><strong style={{ color: "#fff", fontSize: 18, wordBreak: "break-word" }}>{goal.title}</strong>{goal.note && <span style={{ display: "block", color: "#cbd5e1", fontSize: 12, marginTop: 5, wordBreak: "break-word" }}>{goal.note}</span>}</div><strong style={{ color: color.color, fontSize: 22, flex: "0 0 auto" }}>%{goal.progress}</strong></div><div style={{ height: 14, borderRadius: 99, background: "rgba(2,6,23,.45)", marginTop: 14, overflow: "hidden" }}><div style={{ width: `${goal.progress}%`, height: "100%", background: `linear-gradient(90deg,${color.color},#fff)` }} /></div><div style={{ display: "grid", gridTemplateColumns: "1fr", gap: 8, marginTop: 14 }}><input type="range" min="0" max="100" value={goal.progress} disabled readOnly /><div style={{ display: "grid", gridTemplateColumns: "1fr 80px 60px", gap: 8 }}><input style={{ ...inputStyle, padding: "8px", textAlign: "center" }} value={goal.progress} readOnly /><button type="button" style={smallButton} onClick={() => onEdit(goal)}>Düzenle</button><button type="button" style={{ ...smallButton, color: "#fecaca" }} onClick={() => onDelete(goal.id)}>Sil</button></div></div></div>; }
function GoalMini({ goal }) { const color = goalColorInfo(goal.color); return <div style={{ padding: 13, borderRadius: 18, background: `${color.color}22`, border: `1px solid ${color.color}66`, marginTop: 10 }}><strong style={{ color: "#fff", wordBreak: "break-word" }}>{goal.title} • %{goal.progress}</strong><div style={{ height: 8, borderRadius: 99, background: "rgba(2,6,23,.45)", marginTop: 8, overflow: "hidden" }}><div style={{ width: `${goal.progress}%`, height: "100%", background: color.color }} /></div></div>; }
function TaskCard({ task, editId, edit, setEdit, onEdit, onSave, onCancel, onStatus, onDelete }) { const [detailsOpen, setDetailsOpen] = useState(false); const category = categoryInfo(task.category); const priority = priorityInfo(task.priority); const status = statusInfo(task.status); const isEdit = String(editId || "") === String(task.id); if (isEdit) return <div style={{ ...glass, background: category.bg, borderColor: `${category.color}66`, borderRadius: 18, padding: 12 }}><div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(150px,1fr))", gap: 8 }}><Field label="Başlık"><input style={inputStyle} value={edit.title} onChange={(event) => setEdit((current) => ({ ...current, title: event.target.value }))} /></Field><Field label="Kategori"><Select value={edit.category} onChange={(value) => setEdit((current) => ({ ...current, category: value }))} options={CATEGORIES.map((item) => ({ value: item.value, label: `${item.icon} ${item.label}` }))} /></Field><Field label="Öncelik"><Select value={edit.priority} onChange={(value) => setEdit((current) => ({ ...current, priority: value }))} options={PRIORITIES.map((item) => ({ value: item.value, label: `${item.icon} ${item.label}` }))} /></Field><Field label="Tarih"><input style={inputStyle} type="date" value={edit.date} onChange={(event) => setEdit((current) => ({ ...current, date: event.target.value }))} /></Field><Field label="Not"><input style={inputStyle} value={edit.note} onChange={(event) => setEdit((current) => ({ ...current, note: event.target.value }))} /></Field></div><div style={{ display: "flex", justifyContent: "flex-end", gap: 8, marginTop: 12 }}><button type="button" style={smallButton} onClick={() => onSave(task.id)}>Kaydet</button><button type="button" style={smallButton} onClick={onCancel}>Vazgeç</button><button type="button" style={{ ...smallButton, color: "#fecaca" }} onClick={() => onDelete(task.id)}>Sil</button></div></div>; return <div style={{ ...glass, background: category.bg, borderColor: `${category.color}66`, borderRadius: 18, padding: 12, opacity: task.status === "done" ? .8 : task.status === "failed" ? .68 : 1 }}><div style={{ display: "flex", gap: 10, alignItems: "flex-start" }}><div style={{ width: 28, height: 28, borderRadius: 10, display: "grid", placeItems: "center", background: "rgba(255,255,255,.16)", color: status.color, flex: "0 0 auto" }}>{status.icon}</div><button type="button" onClick={() => setDetailsOpen((value) => !value)} style={{ border: 0, background: "transparent", padding: 0, cursor: "pointer", textAlign: "left", flex: 1, minWidth: 0 }}><strong style={{ color: "#fff", fontSize: 14, lineHeight: 1.35, wordBreak: "break-word" }}>{task.title}</strong><span style={{ display: "inline-flex", marginTop: 7, padding: "5px 9px", borderRadius: 99, background: "rgba(255,255,255,.14)", color: "#fff", fontSize: 11, fontWeight: 800, flexWrap: "wrap" }}>📅 {formatDate(task.date)} • {category.icon} {category.label} • {priority.icon} {priority.label}</span></button></div><div style={{ display: "grid", gridTemplateColumns: "1fr", gap: 7, marginTop: 11 }}><select value={task.status || "pending"} onChange={(event) => onStatus(task.id, event.target.value)} style={{ ...smallButton, width: "100%", borderColor: `${status.color}66` }}>{STATUSES.map((item) => <option key={item.value} value={item.value}>{item.icon} {item.label}</option>)}</select><div style={{ display: "grid", gridTemplateColumns: "repeat(3,1fr)", gap: 7 }}><button type="button" style={smallButton} onClick={() => setDetailsOpen((value) => !value)}>{detailsOpen ? "Kapat" : "Aç"}</button><button type="button" style={smallButton} onClick={() => onEdit(task)}>Düzenle</button><button type="button" style={{ ...smallButton, color: "#fecaca" }} onClick={() => onDelete(task.id)}>Sil</button></div></div>{detailsOpen && <div style={{ marginTop: 10, padding: 10, borderRadius: 14, background: "rgba(2,6,23,.28)", border: "1px solid rgba(255,255,255,.12)", color: "#cbd5e1", fontSize: 12 }}><div>Durum: {status.label} • Tarih: {formatDate(task.date)}</div><div style={{ marginTop: 4 }}>Kategori: {category.label} • Öncelik: {priority.label}</div>{task.note ? <div style={{ color: "#fff", marginTop: 8, wordBreak: "break-word" }}>{task.note}</div> : <div style={{ color: "#94a3b8", marginTop: 8 }}>Not yok.</div>}</div>}</div>; }
function SmallTask({ task }) { const category = categoryInfo(task.category); return <div style={{ padding: 13, borderRadius: 18, background: category.bg, border: `1px solid ${category.color}66` }}><strong style={{ color: "#fff", wordBreak: "break-word" }}>{category.icon} {task.title}</strong><span style={{ display: "block", color: "#dbeafe", fontSize: 12, marginTop: 5 }}>{formatDate(task.date)} • {category.label}</span></div>; }

